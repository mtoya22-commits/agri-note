# 畑ノート v5.6

畝と区画の作付けから「今週やること」と作業手順を自動で出す、家庭菜園の記録アプリです。
記録はスマホに保存され、Googleスプレッドシートと自動で同期されます（複数の端末でも使えます）。

---

## 1. GitHub Pages に置く

1. GitHub で新しいリポジトリを作る（例：`hatake-note`、Public）
2. 次のファイルをアップロードする

   | ファイル | 役割 |
   |---|---|
   | `index.html` | アプリの画面 |
   | `app.js` | アプリの動き |
   | `crops.json` | 作物データ（19種）と霜の平年値 |
   | `sw.js` | 電波がなくても開けるようにする |
   | `manifest.webmanifest` | ホーム画面に追加したときの設定 |
   | `icon-192.png` `icon-512.png` `apple-touch-icon.png` | アイコン |

   `gas/` `src/` `build.js` は置いても置かなくても動作に影響しません（あとで直すときに便利です）。

3. リポジトリの **Settings → Pages** で Source：**Deploy from a branch**、Branch：**main / (root)** → Save
4. 1〜2分で `https://mtoya22-commits.github.io/hatake-note/` のようなURLで開けます

## 2. iPhoneのホーム画面に追加する

Safari で上のURLを開き、共有ボタン → **ホーム画面に追加**。以後はホーム画面のアイコンから開きます。
全画面のアプリとして動き、Safari のタブで開くより保存データが消えにくくなります。

## 3. Googleスプレッドシートとつなぐ（最初の1回だけ）

### スクリプトを用意する

1. Googleドライブで **新しいスプレッドシート** を作る（名前は「畑ノート記録」など）
2. メニューの **拡張機能 → Apps Script** を開く
3. 最初から入っている `function myFunction() {…}` を消して、`gas/Code.gs` の中身をすべて貼り付けて保存

### ウェブアプリとして公開する

1. 右上の **デプロイ → 新しいデプロイ** → 歯車 → **ウェブアプリ**
2. 次のユーザーとして実行：**自分**／アクセスできるユーザー：**全員** → **デプロイ**
3. 「アクセスを承認」→ 自分のアカウントを選ぶ
4. 「このアプリは Google で確認されていません」と出たら **詳細 → 〇〇（安全ではないページ）に移動 → 許可**
   （自分で作ったスクリプトなので問題ありません。このスプレッドシートにだけ書き込みます）
5. 表示された **ウェブアプリのURL**（`https://script.google.com/macros/s/…/exec`）をコピー

### アプリに貼る

畑ノートの **設定** → 「保存先のURL」に貼って **保存してつなぐ**。状態が「同期済み」になれば完了です。

- 2台目の端末（PCやタブレット）も、同じURLを貼るだけで同じ記録が見られます
- 機種変更のときも同じです。URLが分からなくなったら Apps Script の **デプロイ → デプロイを管理** で確認できます
- URLは合鍵の役割をします。人に見せたり、GitHub のファイルに書いたりしないでください

### スプレッドシートの中身

| シート | 内容 |
|---|---|
| `plantings` | 作付け（区画・作物・植えた日・終了日・メモ） |
| `logs` | 作業と収穫（1回の記録が1行。同じ日の収穫も別の行） |
| `frost` | 霜リスクの夜の予報と、翌朝の「霜あり／なし」 |
| `settings` | 作る作物・補正の設定 |

- 集計やグラフは **別のシート** で行ってください（ピボットテーブルなど）
- `deletedAt` に値がある行は、アプリで削除した行です。集計ではフィルタで外してください
- 数字やメモを **シート上で直接直してもかまいません**。直した行は自動で版（version）が上がり、各端末に反映されます
- 右端の4列（`updatedAt` `deletedAt` `version` `changeSeq`）は同期用です。触ろうとすると警告が出ます
- 1行目の見出しと列の並びは変えないでください

### 同期のしくみ（困ったときのために）

- 変更はまず端末に保存し、つながったときに送ります（畑で電波がなくても記録できます）
- アプリを開いたときに、スプレッドシートの全件を取り込みます
- 2台の端末（またはシートの手修正）で同じ行を変えていた場合は、**あとから変えた方** を残します
  （行単位なので、同じ作付けの別の項目を2台で同時に直した場合、古い方の変更は消えます）
- どうしても合わなくなったら、設定の **スプレッドシートから作り直す** で端末の内容を作り直せます

## 4. 作物データを直す

`crops.json` を GitHub 上で直接編集できます（鉛筆アイコン）。

| 項目 | 意味 |
|---|---|
| `start.window` | 植え付け・播種の適期（`"5中"` ＝5月中旬）。中間地の標準 |
| `start.notBefore` | 霜に弱い作物は「終霜から何日後以降」（`{"anchor":"lastFrost","days":14}`） |
| `tasks[].trigger` | `offset`：植えた日から◯日後（マイナスは準備作業）／`calendar`：暦で固定 |
| `trigger.repeat` | `everyDays` 日ごとに、`untilDays` 日目まで繰り返す |
| `trigger.notAfter` | 霜の前に終える作業（さつまいもの掘り上げ：初霜の7日前まで） |
| `frostKilled` | 初霜で枯れる作物。収穫期と見回りは初霜で打ち切り |
| `rotation` | 同じ区画で同じ科を作るまでの年数の幅と出典。`minYears` 未満＝連作リスク高、`maxYears` 未満＝推奨より短い |
| `routine` | 見回り系。まとめて完了でき、遅れ表示しない |
| `slotsNeeded` | 必要な区画数（1区画＝1/4畝） |
| `region` | 霜の平年値（初霜 11/24・終霜 4/1、気象庁 岐阜地方気象台）と予報地点 |

`"` や `,` の抜けがあると読み込めなくなります。保存前に [JSONの検証サイト](https://jsonlint.com/) で確かめると安全です。

### 連作年数の出典

作物ごとに `rotation.source` に記載しています。資料によって年数がかなり違うため、1つの資料を主に採用しています。

- 岡山県「野菜の年間栽培計画をたてよう！」：トマト・ナス・ピーマン・枝豆 4〜5年、キャベツ・イチゴ 2年、ほうれん草・レタス 1年、大根・にんじん・カブ・玉ねぎ 連作の害が少ない
- JA秋田ふるさと 営農ワンポイントアドバイス（2024年10月）：オクラ 1〜2年、スイカ 6〜7年、さつまいも・トウモロコシ 連作障害が少ない（トマト・ナスは6〜7年としている）
- アタリヤ農園「連作障害と輪作」：ブロッコリー 1年
- バジルは出典が見つからず暫定値（1年）です

### 霜の安全幅について

霜に弱い作物の植え付けを「平年の終霜から14日後以降」、掘り上げを「初霜の7日前まで」としているのは、アプリの暫定の安全幅です（出典なし）。
なお、タキイ種苗は夏野菜の定植の目安を「晩霜の心配がなく最低気温10℃以上、最低地温15℃以上」としています。多くの年は、中間地の適期（5月上〜中旬）がこれに先に当たります。

## 5. アプリを更新したとき

`sw.js` の1行目 `CACHE = "hatake-note-v5.6"` の番号を1つ上げてからアップロードしてください。
スマホ側は、アプリを **2回開き直す** と新しい版になります。

## 6. 霜リスクについて

- 各務原市の1時間ごとの予報（[Open-Meteo](https://open-meteo.com/)・無料・登録不要）から、夜ごとの最低気温・夜明け前の雲量と風速を見て判定します
  - **高**：最低1℃以下、または 3℃以下で「晴れ（雲30%以下）・弱風（2m/s以下）」
  - **中**：最低3℃以下、または 5℃以下で「晴れ・弱風」
  - この目安は暫定です。晴れて風の弱い夜は放射冷却で、地面近くが予報気温より冷えやすくなります
- 霜に弱い作物が植わっていて、7日以内に中・高の夜があるときだけ「今週」タブに出ます
- 中・高の夜は、前の晩の予報をそのまま保存します。翌朝に1回だけ「霜は降りていましたか？」と聞きます
- 答えは「記録」タブの **霜の記録** とスプレッドシートの `frost` シートにたまります。たまったら判定の見直しに使えます

## 7. 適期を過ぎた作物・作付けの始め方

### 時期の状態

| 表示 | 意味 |
|---|---|
| **適期中** | 中間地の適期（補正・霜の制約をかけたもの） |
| **遅め** | 適期は過ぎたが、`lateUntil` までは始められる。「作付けの適期」に残り、「今から始める」で登録できる |
| **◯日後** | 次の適期まで60日以内 |
| **今季非推奨** | それ以外。適期の一覧には出ないが、区画から選べば登録できる（初期の日付は次の適期の初日） |

`lateUntil` を入れているのは、資料で確認できた4作物だけです（`start.lateSource` に出典）。

| 作物 | 遅めの期限 | 出典 |
|---|---|---|
| 大根 | 9月下旬 | サカタのタネ「冬自慢」一般地・暖地 9月上旬〜下旬まき（品種による） |
| ブロッコリー | 9月下旬（植えつけ） | JA西春日井 家庭菜園：植えつけ 8月下旬〜9月下旬 |
| 晩生玉ねぎ | 12月上旬（植えつけ） | JA西春日井 家庭菜園：中生〜中晩生 11月中旬〜12月上旬 |
| ほうれん草 | 11月下旬 | 群馬県 ぐんまアグリネット（品種により8月上旬〜3月中旬まき）。11月下旬はアプリの控えめな区切り。寒い時期は保温 |

にんじん（JA西春日井：7月中旬〜8月中旬＝今の適期と同じ）、カブ・リーフレタス・キャベツ（はっきりした期限が見つからず）は、適期を過ぎると「今季非推奨」になります。

### 登録＝計画、開始作業の完了＝実際のスタート

- 作付けを登録しただけでは、播種・定植は完了になりません。今週のやることに残ります
- 登録する日付が今日以前のときは「これからまく／もうまいた」を選びます（過去の日付は「もうまいた」、今日は「これからまく」が初期値）
- 播種・定植を「完了」にするとき、実施した日を選べます。**その日から**、間引き・追肥・収穫などを計算し直します
- 予定日を過ぎても未実施のときは「開始待ち」に出し、その後の作業はまだ出しません
- 開始が遅れると、区画を使う期間の終わりも後ろにずれます。次の作付けの予約と重なったら「区画の予定が重なっています」と出ます

### 事前準備の確認

予定日より前に済ませておくはずだった準備作業（土づくりなど）が過ぎてしまった場合は、「遅れ」として出さずに、播種・定植の手順画面の上に「事前準備の確認」としてまとめます。

- 初期値では **「本来は播種の21日前ごろの工程」という情報として見せるだけ** です。今からそのまま行えという意味にはしません（石灰の「2週間置く」など）
- 本来の手順は「本来の手順を見る」を開くと確認できます
- 作業ごとに `lateStart` で扱いを指定できます（資料で確認してから入れる想定です）

| `lateStart.mode` | 表示 |
|---|---|
| `noteOnly`（初期値） | 本来の工程として情報だけ見せる |
| `doNow` | 「今からでもやること」として `doNow` の手順を見せる |
| `skip` | 表示しない |

`message` を入れると、その一言も表示します。例：`"lateStart": {"mode":"doNow","message":"播く前に畝だけは作る","doNow":["畝を立てて表面をならす"]}`

### 生育期間が足りない見込み

遅れて始めた場合も霜の制約は無視しません。霜で枯れる作物で収穫開始の見込みが平年の初霜より後になるとき、さつまいもで掘り上げの期限までに育ちきらないときは、強い警告を出します（登録はできます）。

## 8. 週1〜2回の畑通いに合わせた表示

日付は「締め切り」ではなく「目安」です。保存する日付（実際にまいた日・間引いた日・収穫した日）は正確に残し、判断のほうに幅を持たせています。

### 作業の種類と許容幅

作業の種類は作業名ではなく、作物データの項目から決めています。日数はアプリの初期値（出典なし）で、作業ごとの `timing: {"earlyDays":…, "lateDays":…}` で上書きできます。

| 種類 | 決め方 | 目安日の前 | 目安日の後 |
|---|---|---|---|
| 開始（播種・定植） | 植えた日から0日の作業 | — | 予定日を過ぎたら「開始待ち」 |
| 準備 | 植えた日より前の作業 | 3日 | 7日（それより前のものは「始める前に確認」へ） |
| 管理 | 間引き・追肥・土寄せ・誘引・整枝など | 3日 | 7日 |
| 収穫（1回きり） | 繰り返さない収穫 | 3日 | 7日 |
| 期限つき | `notAfter` がある作業 | 7日 | 期限の日まで |

- 許容幅は **期限を越えません**（許容幅 ∩ 期限）。例：玉ねぎの止め肥は、注意書きの「3月以降の追肥は厳禁」を期限（2月末）としてデータに入れました
- `urgency: "high"` の作業（今は期限つきの作業）は、個別の指定がなければ許容幅を短くします
- 見回り系と収穫期は、これまでどおり「遅れ」扱いにしません

### 生育段階がある作業

「本葉1〜2枚」などの段階がある作業は、日付を **確認を始める目安**、段階を **実施する条件** として表示します（例：`間引き1回目　9/30ごろ〜・本葉1〜2枚になったら`）。日付を過ぎても「遅れ」にはしません。

許容幅（初期値は目安日の7日後）を過ぎても未完了のときは、「遅れ」とは断定せずに **「生育を確認」** と表示し、「予定の時期を過ぎています。今の生育を確認してください」と案内します。目安日から3週間を過ぎると、ほかの作業と同じく「見送り」にできる一覧に移ります。

### 収穫期と収穫の頻度

収穫期に入っている作物は、次の収穫日にかかわらず「収穫期」に並びます。収穫の繰り返し間隔（`repeat.everyDays`）が7日より短い作物は、「2日おきが目安。週1〜2回だと採り遅れが出やすい作物です」と表示します（収穫期に入っている間だけ）。

### 実施日

すべての完了画面と「まとめて見回り済みにする」に、実施日（今日・昨日・日付を選ぶ）があります。普段は「今日」のまま1タップで完了できます。選んだ日付が保存され、次の作業の計算（間引き・次の収穫など）もその日から行います。実施日は事実なので **今日より先の日付は選べません**（予定は作付けの予定日として別に持ちます）。

## 9. 表示（ライト／ダーク）

設定タブの「表示」で、**自動／ライト／ダーク** を選べます。「自動」はスマホ本体の設定に合わせます。明るい屋外ではライトが読みやすいです。

- この設定は **端末ごと** で、スプレッドシートには同期しません（夫婦で別の表示にできます）
- ライト表示の補助文字・線・「もうすぐ」の色を濃くして、直射日光の下でも読みやすくしました（v4.3）

## 10. おすすめの畝と、畝の選び直し（v4.4）

「空き区画に植える」は、空いている全部の場所に点数をつけて一番良い場所を選びます（小さいほど良い）。

| 条件 | 点 |
|---|---|
| 連作リスク高（赤） | +100 |
| 黄色の注意（連作やや短い・日照注意・遅霜・適期外など） | +10 |
| 半日陰でも育つ作物を畝cへ | −5 |
| 冬を越す作物（印なし）を畝cへ | +3（警告は出さず、少し後回しにするだけ） |
| 区画が使用中 | 候補にしない |

- **日当たりは冬だけ判定**：畝cが半日陰なのは冬（11/1〜2月末）なので、栽培期間が冬に30日以上かかる作付けだけ日当たりを見ます。夏作は畝cも他の畝と同じ扱いです
- 玉ねぎ（晩生）とイチゴは `winterSunRequired: true`（冬の日照が必要）。半日陰の畝では「日照注意」が出ます
- 注意のない場所が無いときは、一番軽い注意の場所を選び、理由を表示します
- 登録画面で **畝を選び直せます**。畝ごとに「おすすめ／注意あり／連作注意／空きなし」を表示し、選んだ畝の中で一番良い区画を自動で選びます（区画は手で変えられます）

## 11. 畝の中の位置（v5・段階1：中身の入れ替え）

作付けは「区画（1/4畝）」ではなく **畝の中の位置（spots）** で判定するようになりました。画面はまだ4区画のままです（段階2で畝の中に株を置けるようにします）。

**畝の寸法**：設定タブの「畝の寸法」で変更できます（初期値は概算：a・b 500×70cm、c 400×70cm）。

**位置の持ち方**（保存するのは事実としての位置だけ。株間は作物の条件として判断のたびに計算）

| 種類 | 保存するもの | 例（シートの spots 列） |
|---|---|---|
| 株 | 植えた中心（左端からのcm） | `株170` |
| まとまり | 範囲と株数 | `群300-390×3` |
| 帯 | 範囲 | `帯300-390` |

修飾：`/幅0-35`（幅方向。未指定＝畝の幅全体）、`/終2026-08-01`（その位置だけ終わった日）、`/区画`（区画から変換した粗い位置）。区切りは半角スペース。

- 位置を持たない作付け（v4までのデータ）は、**読むたびに** 区画から位置へ変換します（シートには書き戻しません）。畝の長さを直すと、変換も自動で合います
- 同じ場所の判定：株どうしは中心が10cm未満、株が帯の中、範囲どうしが重なる（端が接するだけは重ならない）。長さ方向と幅方向の両方が重なったときだけ
- 連作の判定：株は株間ぶんの広がり、畝の中で置いた位置には根の余白10cm（アプリの仮の目安）を足した範囲の重なりで判定。区画から変換した位置どうしは、今までどおり区画単位
- 一部の位置だけ終わった場合は、その位置だけが空きます（作付けは続行）
- シートの spots 列に読めない書き方があっても、その文字は消さずに残し、区画の位置で動きます

**スクリプトの更新**：`gas/Code.gs` を v5 に差し替えると、plantings シートの右端に `spots` 列が自動で追加されます（既存の行はそのまま）。v4 のスクリプトのままでも同期はできますが、段階2の前に更新が必要です（設定画面に表示されます）。

## 12. 畝の中に株を置く（v5.1・段階2の最初の区切り）

**置き方**
- 畑タブの畝のバー（横長の帯）の、空いている所をタップ →「株で置く作物」を選ぶ → 位置を確認して「置く」
- 置いてある株をタップ →「◀ 左に植える」「右に植える ▶」→ 作物を選ぶ
- 「＋ 株を置く」ボタン：左から見て、重ならず株間も足りる最初の場所に置く
- 位置は ◀ ▶ で5cmずつ動かせます。両隣の株までの距離を表示します

**同じ作付けに1株追加**：作物・畝・開始日・品種がすべて同じ作付けがあれば、「〇〇（5/10・桃太郎・1株）に1株追加」を初期選択にします。外せば別の作付けになります（勝手にはまとめません）。追加した株は、その作付けのやること・収穫をそのまま使います。

**株間**（`crops.json` の `spacingCm`。出典の文章 `start.spacing` から数値を取り、文章は残す）
- `min`＝必要な株間、`preferred`＝推奨（「30〜35cm」のように幅があるときだけ）
- 違う作物どうしの目安は、両方の必要な株間の平均（例：トマト45とバジル30 → 38 → 5cm刻みで40cm）。同じ作物どうしはその作物の株間
- 目安より近いと「株間が狭い」（黄色の注意。置くことはできる）。中心が10cm未満は「同じ場所」で置けない
- **株間が分からない作物は、株間の注意もおすすめの材料にも使いません**（仮の数字を知っていることにしない）。表示は「株間の目安なし」。連作の範囲は「株の位置＋根の余白10cm」だけ

**安全策**
- シートの位置（spots）が読めず、区画もない作付けは、畝全体を使っているものとして扱います（空きにしない・おすすめしない）。「位置不明」と表示し、元の文字は消しません
- 畝の長さを短く直すと、畝の外に出る株を警告します。**位置は自動では動かしません**。変更後は「畝の外」と表示します（区画から変換した古い位置は、今までどおり1/4に合わせて変わります）
- 同期しているとき、スプレッドシート側の Code.gs が v5 でなければ株は置けません（位置がシートに残らないため）。v5 の Code.gs は `spots` と `variety`（品種）列を自動で追加します

## 13. まとまり・帯（v5.2）

作物を選ぶと **株／まとまり／帯** を選べます（すじまき・帯の作物〈ほうれん草・にんじん・カブ・玉ねぎ〉は帯だけ）。

| 植え方 | 操作 | 保存するもの |
|---|---|---|
| 株 | ◀ ▶ で5cmずつ | 植えた中心 |
| まとまり | 株数 − ＋、条（1条・2条）、◀ ▶ | 占有する栽培スペースと株数・条数（長さ＝株数÷条数（切り上げ）×株間） |
| 帯 | 長さ − ＋（10cmずつ）、◀ ▶ | 範囲。株数・本数は任意 |

- **まとまりの範囲（from〜to）の定義**：株が並んでいる範囲（最初〜最後の株の中心）ではなく、両端の半株間を含めた「その群植が占有する栽培スペース」。5株・2条・株間30cmなら90cm（15｜●─30─●─30─●｜15）。個々の株の位置は保存せず、株数・条数・スペースから必要なときに計算する（1条目 ●●●／2条目 ●●）。シートでは `群320-410×5/2条`
  - この定義を、連作（スペース＋根の余白10cm）、隣への配置（異種の株の中心どうしが両者の株間の平均になる）、同じ場所の判定（スペースの中には株を置けない）、今後の一部終了（株数を減らし、スペースは残す）で一貫して使う
  - 株もこの考え方と揃っている：株の栽培スペースは中心±株間の半分（連作の範囲に使う）。ただし株は中心の位置が分かるので、「同じ場所」は中心の差10cm未満だけにし、近いときは株間の注意（黄色）にとどめる
- まとまりの既定の条数は出典の文章にあるものだけ（トウモロコシ「必ず2条以上」→ 2条）。ほかは1条
- **タップした所が初期位置**（まとまり・帯はその所を中心に）。「＋ 株を置く」など位置の指定がないときだけ、左から空いている所を探す（株は畝の端から株間の半分あける）
- 置いた株・まとまり・帯をタップすると「左に植える／右に植える」

**隣に置くときの初期位置**
- 株と株：両方の株間が分かれば平均。片方だけ分かれば、分かっている側の株間を初期の距離にして「株間は要確認」と表示。どちらも分からなければ、重ならない最小の距離に「仮置き」するだけ（推奨の距離とは言わない）
- 株の隣にまとまり・帯：その株の株間の半分をあける。まとまり・帯の隣に株：置く株の株間の半分をあける（分からなければ「仮置き」）
- まとまり・帯どうし：接して並べる
- 株間が分からない作物のまとまりは、長さを決めず「仮の長さ」として手で合わせる

株間の注意（黄色）は株どうしだけで判定します。連作・同じ場所の判定は、まとまり・帯の範囲で行います。

## 14. 左から順に登録・位置の修正・置いたときの株間（v5.3）

**左から順に登録**（畝のカードの「左から順に登録」）
- 植えた順に作物をタップすると、前のものの右に並びます（株の作物は1株ずつ、すじまきの作物は1mの帯）。並べ方は「隣に置くときのきまり」と同じ（株と株は両方の株間の平均など）
- 畝にすでに作物があれば、いちばん右の作物の続きから並べます
- 畝の長さを超えると登録できません（「1つ戻す」で減らす）
- 同じ作物は1つの作付けにまとめます。**作物・畝・実際の開始日・品種**がすべて同じ作付けがその畝にあれば、そこへ追加します（チェックを外せば新しい作付け）。品種は「登録の内容」で作物ごとに入れられます（v5.4）。登録の前に内容を表示します
- 例：ナス・ナス・ピーマン・バジル・トマト・バジル・トマト・バジル・トマト・トウモロコシ×3 → 5作付け・12株。畝a（500cm）にちょうど収まります（最後のトウモロコシが485cm）

**位置を直す・消す**（置いた株・まとまり・帯をタップ）
- 「位置を直す」：その位置だけを ◀ ▶ で動かす（まとまりは株数・条・長さ、帯は長さも）。同じ作付けのほかの株とも重なりを調べます
- 「この位置を消す」：置き間違いの修正用。株数も減ります。作付けの最後の位置は消せません（作付けごと消すときは「修正・メモ・削除」から）
- 枯れた・収穫が終わった株の記録（一部終了）は次の区切りで作ります

**置いたときの株間（スナップショット）**
- 株を置いたときの株間（推奨）を、その株に記録します（シートでは `株200/間45`）
- あとで作物データの株間を直しても、過去に置いた株の栽培スペース（連作の範囲）は変わりません。新しく置く株と、おすすめ・株間の注意には、今の作物データを使います
- 記録がない株（v5.2 までに置いた株）は、今の作物データで計算します

**一部終了の設計（次の区切り）**：まとまりの `n`（植えた株数）は、中の株の位置を計算するための事実として変えません。「1株減った（どれか不明）」は別の数（失った数）として持ち、スペースはそのまま使用中とします。「この株が終了（位置が分かる）」は、終わった株を個別に記録できる形にします。

## 15. 一部終了（v5.4）

置いた株・まとまり・帯をタップ →「枯れた・抜いた・収穫が終わった」

| 対象 | 操作 | 記録 | 場所 |
|---|---|---|---|
| 株・帯 | この株（帯）を終える | その位置に終了日（シート `/終2026-07-20`） | 終了日から空く |
| まとまり | 1株減った（どれか不明） | `lost`＋1（シート `/減1`）。植えた株数 `n` は変えない | 使用中のまま |
| まとまり | 株を選んで終える（1条目の3株目 など） | `ended` に条・何株目・日付（シート `/終株1-3@2026-07-20`） | 使用中のまま |

- **まとまりの個別の終了は、記録の精度を上げるためのもの**です。全部の株が終わる（生育中が0になる）まで、まとまりの場所は使用中のままです。その場所をすぐ別の株に使いたいときは、まとまりを株として置き直してください
- **二重に数えない**：「どれか不明」で減らしたあとで、どの株だったか分かったときは「不明な減少のうち1株を、この株の記録に振り替える」（初期はオン）で `lost` を1つ減らしてから `ended` に記録します。生育中が0なのに特定の株を終えるときは、自動で振り替えます（マイナスにしない）
- 生育中の株数＝植えた株数 − 不明な減少 − 終わった株
- **全部の位置が終わったら、作付けも終了**にします（終了日は最後の位置の終了日）
- 作付けの詳細に「位置（植えた◯株・生育中◯株）」を表示し、終了の記録はそこから取り消せます（記録の間違い用）。取り消すと作付けも再開します
- 「作付けを再開」は、最後に終わった位置だけを戻します。それより前の一部終了は残ります
- 「この位置を消す」（置き間違いの修正＝そもそも植えていなかった）とは別の操作です。終了した位置は連作の履歴に残ります

## 16. おすすめの位置（v5.5・段階3）

「作付けの適期」の **おすすめの場所に植える**、畝の **＋ 株を置く**、置く画面の **この畝のおすすめの位置へ** で使います（区画でかんたん登録のおすすめは今までどおり）。

**1. 候補を作る**：株は中心、まとまり・帯は左端を5cm刻みで。物理的に置けない候補（畝の外・同じ場所）は外します。同じ場所の判定は一部終了のきまりのまま（株・帯は終了日から空き、まとまりは全部の株が終わるまで使用中）。

**2. 上から順に比べる**（同じなら次の項目へ）
1. 連作の赤があるか
2. 赤の重なり率（連作の範囲のうち、赤の相手と重なる割合）
3. 黄色の注意の数（株間が狭い・連作やや短い・日照・遅霜・適期外など）
4. 日照の相性（冬に30日以上かかる作付けで、半日陰向きの作物が畝c → 良い／それ以外の作物が畝c → 悪い）
5. 黄色の連作の重なり率
6. 半端なすき間の数（「作る作物」の株の作物のうち、いちばん小さい株間より狭いすき間）
7. 隣の作物・畝の端に接する数（多いほど良い）
8. 畝の順（a→b→c）→ 左から

**3. 理由**：上の比較で実際に候補をふるい落とした項目（上の項目まで同じ候補のうち、その項目で1位より悪い候補があった項目）を最大2つ、「決め手」として表示し、1位の評価結果（連作・株間・注意・日照・すき間・接する数）を並べます。おすすめと説明が食い違わないよう、理由は評価の結果からだけ作ります。

**確認したケース**：左だけ連作の赤 → 右を選ぶ／赤しかない → 重なりの小さい右端（27%）／冬を越すキャベツは畝cを選ばない・半日陰向きのほうれん草は畝c／条件が同じなら20cmの半端な空きを残さない（v5.4までの「左から最初」なら残していた）／終わった株の場所は候補になり、一部だけ終わったまとまりは候補にならない

## 17. おすすめと実際の記録・確信度（v5.6）

**おすすめと実際の記録**：株・まとまり・帯を置くたびに、その時のおすすめの位置と、実際に置いた位置を記録します（`settings` の `recLog`。スプレッドシートの settings 行に同期。最大300件。スクリプトの更新は不要）。
- 記録する内容：日付・作物・植え方・おすすめ（畝・位置）・実際（畝・位置）・おすすめどおりか・確信度・どこから置いたか（適期／株を置く／タップ／隣）
- 設定タブの「おすすめと実際」に、おすすめどおりの件数・変えた件数と、変えたときの直近5件を表示
- 理由の入力はしません。たまった記録から「毎回〇〇で変えている」傾向を見て、実際にズレが出た理由だけ評価の項目に足します（先回りで項目を増やさない）

**確信度**（内部で持つ。表示は1行だけ）：1位と、離れた別の場所（別の畝か50cm以上離れた所）の最良の候補を比べ、最初に差がつく項目で決めます。
- 連作の赤・赤の重なり・注意の数で差がつく → high
- 日照・黄色の連作で差がつく → mid
- すき間・接する数・並び順でしか差がつかない → low。「ほかにもほぼ同じ条件の場所があります（〇〇など）」と表示
- 置ける場所がほかにない → only
